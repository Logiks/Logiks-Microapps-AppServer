//LogiksAI Integration Layer for AICore

const AIEngine = require("./aiengine");

module.exports = class LogiksAI extends AIEngine {
    
    // constructor(params = {}) {
    //     this.params = params;
    // }
    
    // async sendMessage(sessId, message, userInfo, params = {}) {
    //     console.log("LogiksAI", this.params);
    //     return false;
    // }
}